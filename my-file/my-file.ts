//const age: number = 20;
//Déclare une constante age de type number

function addition(x: number, y: number): number {
    return x + y;
}
const result: number = addition(10, 20);
console.log(result);
